---
applications:
  .:
    name: FR_Foosball
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    infra: aws
    url: ${name}.${target-base}
    mem: '512'
    instances: 1
